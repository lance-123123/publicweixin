 /*点击添加标签事件*/
     /* $("#imgset").click(function(){
          art.dialog.prompt('请输入标签名', function (val) {
	          art.dialog.tips(val);
            }, '');
      });*/

  
  /*把博客设置为私有*/
  /*$(".public_artical").on('click',function(){
       
  
  $.ajax({
       	  url:$('.public').attr('url'),
       	  type:'post',
       	  data:{'id':$('.public_artical').attr('val')}

       	  success : function(data) {
       	  	if (data == 1) {
       	  		art.dialog.tips('添加失败！');
       	  	}else if (data==0) 
       	  	{
       	  		art.dialog.tips('添加成功！');
       	  	    document.location.reload();
       	  	}
       	  }
       	  
       }); 
  }); */

